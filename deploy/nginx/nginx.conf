
events {}

http {
    include       mime.types;
    default_type  application/octet-stream;

    # Рекомендуемые настройки безопасности
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    server {
        listen 443 ssl;
        # server_name localhost;
        server_name time-inside.ru www.time-inside.ru;  # домен

        # Пути к сертификатам
        # ssl_certificate /etc/nginx/certs/fullchain.pem;
        # ssl_certificate_key /etc/nginx/certs/privkey.pem;
        ssl_certificate /etc/letsencrypt/live/time-inside.ru/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/time-inside.ru/privkey.pem;

        # Оптимизация SSL (например, TLS 1.2 и 1.3)
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384';
        ssl_prefer_server_ciphers off;
        
        # Параметры для клиента
        client_max_body_size 10M;

        # ==========================
        # 📡 API проксирование
        # ==========================
        location /api/ {
            proxy_pass http://policy-enforcement-service:5003;  # Прокси на бэкенд
            rewrite ^/api/(.*)$ /$1 break;  # Удаляет /api из пути

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Прокси Swagger-документации
        location /api/docs {
            proxy_pass http://policy-enforcement-service:5003/docs;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Прокси OpenAPI схемы
        location /openapi.json {
            proxy_pass http://policy-enforcement-service:5003/openapi.json;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        # # ==========================
        # # 🖥️ Фронтенд (SPA)
        # # ==========================
        # location / {
        #     root /usr/share/nginx/html;  # Путь до собранного фронтенда (например, React build)
        #     index index.html;

        #     try_files $uri $uri/ /index.html;  # SPA: пробрасываем всё в index.html
        # }
    }

    server {
        listen 80;
        # server_name localhost;
        server_name time-inside.ru www.time-inside.ru;  # домен

        # 🔐 Сервер для ACME-челленджа (http-01)
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
        
        # 👉 Редирект всего остального на HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }
}

